This repository is currently docs-only. No package manifests, runnable entrypoints, test commands, lint commands, or build scripts are present yet. For repo exploration on Windows, useful commands include: `Get-ChildItem`, `rg --files docs`, and Serena tools for reading markdown/docs. When implementation begins, this memory should be updated with actual monorepo commands for backend, web, mobile, worker, lint, test, migration, and docker workflows.