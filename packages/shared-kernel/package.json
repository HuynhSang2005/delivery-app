{
  "name": "shared-kernel",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "bun run typecheck",
    "test": "bun run build"
  },
  "nx": {
    "tags": [
      "scope:shared",
      "type:util",
      "platform:shared"
    ]
  }
}
